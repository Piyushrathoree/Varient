# /bootstrap - Project Bootstrap
Run once per project to set up the complete AI tooling structure. Re-run only when topology changes.

## Instructions
Read the workflow defined in `.agents/workflows/bootstrap.md` and execute it.
