# /session-handoff - End-of-Session Handoff
Run at the end of every session to update CONTEXT.md, detect new workflow patterns, and flag stale skills.

## Instructions
Read the workflow defined in `.agents/workflows/session-handoff.md` and execute it.
