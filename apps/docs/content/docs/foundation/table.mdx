---
title: Table
description: Semantic compound table for structured data — bordered wrapper, muted header row, row dividers, and hover states on body rows.
---

<ComponentPreview name="table" />

## Installation

Copy the component from `packages/ui/src/components/foundation/table/` into your project.

```bash
bun add clsx tailwind-merge
```

## Usage

```tsx
import { Table } from '@/components/foundation/table';

export function Example() {
  return (
    <Table>
      <Table.Caption>A list of your recent invoices.</Table.Caption>
      <Table.Header>
        <Table.Row>
          <Table.Head>Invoice</Table.Head>
          <Table.Head>Status</Table.Head>
          <Table.Head className="text-right">Amount</Table.Head>
        </Table.Row>
      </Table.Header>
      <Table.Body>
        <Table.Row>
          <Table.Cell className="font-medium">INV-001</Table.Cell>
          <Table.Cell>Paid</Table.Cell>
          <Table.Cell className="text-right">$250.00</Table.Cell>
        </Table.Row>
        <Table.Row>
          <Table.Cell className="font-medium">INV-002</Table.Cell>
          <Table.Cell>Pending</Table.Cell>
          <Table.Cell className="text-right">$150.00</Table.Cell>
        </Table.Row>
      </Table.Body>
    </Table>
  );
}
```

Use `Table.Footer` for summary rows and set `scope="row"` on `Table.Head` when labelling row headers.

```tsx
<Table.Row>
  <Table.Head scope="row">Total</Table.Head>
  <Table.Cell colSpan={2}>$400.00</Table.Cell>
</Table.Row>
```

`variant`, `size`, and `isStickyHeader` live on the root `Table` and flow down to every cell through
context, so `Table.Head` / `Table.Cell` padding and `Table.Body` row striping stay in sync automatically:

```tsx
<Table variant="striped" size="compact" isStickyHeader className="max-h-72">
  {/* ... */}
</Table>
```

## Props

<PropsTable
  props={[
    {
      name: 'Table',
      type: 'HTMLAttributes<HTMLDivElement>',
      description: 'Root wrapper — rounded-xl border, overflow-x-auto, and a full-width table inside. Provides variant/size/isStickyHeader context to all children.',
    },
    {
      name: 'variant',
      type: "'default' | 'striped'",
      defaultValue: "'default'",
      description: '`striped` tints odd `Table.Body` rows with bg-muted/30 (Table root).',
    },
    {
      name: 'size',
      type: "'default' | 'compact'",
      defaultValue: "'default'",
      description: '`compact` tightens `Table.Head` height (h-8 px-3) and `Table.Cell` padding (px-3 py-1.5) (Table root).',
    },
    {
      name: 'isStickyHeader',
      type: 'boolean',
      defaultValue: 'false',
      description: 'Pins `Table.Header` to the top of the root\'s scroll container with sticky top-0, bg-background/95, and backdrop-blur (Table root).',
    },
    {
      name: 'Table.Header',
      type: 'HTMLAttributes<HTMLTableSectionElement>',
      description: '`<thead>` section — header cells use bg-muted/50; becomes sticky when the root sets isStickyHeader.',
    },
    {
      name: 'Table.Body',
      type: 'HTMLAttributes<HTMLTableSectionElement>',
      description: '`<tbody>` section — rows get hover:bg-muted/30 and hairline dividers; odd rows tint too when the root sets variant="striped".',
    },
    {
      name: 'Table.Footer',
      type: 'HTMLAttributes<HTMLTableSectionElement>',
      description: '`<tfoot>` section — muted background for totals or summary rows.',
    },
    {
      name: 'Table.Row',
      type: 'HTMLAttributes<HTMLTableRowElement>',
      description: 'Table row — border-b border-border divider between rows.',
    },
    {
      name: 'Table.Head',
      type: 'ThHTMLAttributes<HTMLTableCellElement>',
      description: 'Header cell — defaults to scope="col", text-muted-foreground, font-medium, text-left; height/padding follow the root\'s size.',
    },
    {
      name: 'Table.Cell',
      type: 'TdHTMLAttributes<HTMLTableCellElement>',
      description: 'Data cell — align-middle; padding follows the root\'s size (px-4 py-3 default, px-3 py-1.5 compact).',
    },
    {
      name: 'Table.Caption',
      type: 'HTMLAttributes<HTMLTableCaptionElement>',
      description: 'Table caption — describes the table for assistive tech; renders below the table.',
    },
  ]}
/>

## Accessibility

- Use `Table.Caption` to provide a concise description of the table contents.
- Set `scope="col"` on column headers (the default on `Table.Head`) and `scope="row"` on row headers.
- Pair header cells with data cells using `id` on headers and `headers` on cells when relationships are non-trivial.
- The root wrapper uses `overflow-x-auto` so wide tables scroll horizontally on narrow viewports without breaking layout.
- `isStickyHeader` only affects visual position (CSS `position: sticky`); it doesn't reorder the DOM, so it has no effect on how assistive tech reads the table.

| Element | Purpose |
| ------- | ------- |
| `<caption>` | Summarizes the table for screen readers |
| `scope="col"` | Associates a header cell with its column |
| `scope="row"` | Associates a header cell with its row |
| `headers` | Links a data cell to one or more header cell ids |
