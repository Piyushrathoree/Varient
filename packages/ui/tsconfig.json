{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "ignoreDeprecations": "6.0",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  "//": "*.native.tsx needs React Native + Reanimated types; it is typechecked by the mobile app, not by this web-oriented package typecheck.",
  "exclude": ["node_modules", "src/**/*.native.tsx"]
}
